import 'package:flutter/material.dart';

class RegisterMedecinScreen extends StatelessWidget {
  const RegisterMedecinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Inscription Médecin')),
      body: const Center(
        child: Text('Inscription Médecin - À implémenter'),
      ),
    );
  }
}
